env:
##Do not define 'global' env vars here.  They cannot be used with API builds
    matrix:
      - TARGET=devo8
      - TARGET=devo10
# use new docker environment
sudo: false

#sudo: required
#dist: trusty

addons:
  apt:
    packages:
      - build-essential
      - git
      - inkscape
      - libjpeg-dev
      - libz-dev
language: python

# command to install dependencies - this is done in Makefile already
#install: "pip install -r requirements.txt"

script:
    - make html pdf

cache:
    pip: true
    directories:
    - $HOME/build/venv

after_success:
# Here we have to put automatic manual uploading code
